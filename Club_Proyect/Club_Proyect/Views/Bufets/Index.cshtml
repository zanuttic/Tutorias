@model IEnumerable<Club_Proyect.Entities.Bufet>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.cliente.persona.Nombre)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.cliente.persona.Apellido)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.cliente.Num_Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.venta.totalVenta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.venta.Fecha)
            </th>



            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.cliente.persona.Nombre)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.cliente.persona.Apellido)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.cliente.Num_Cliente)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.venta.totalVenta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.venta.Fecha)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
